.col-lg-12.col-md-12.col-sm-12.col-xs-12.privacy
  .row
    .col-lg-10.col-lg-offset-1.col-md-10.col-md-offset-1.col-sm-10.col-sm-offset-1.col-xs-12
      .btn-group.btn-group-justified{ role: 'group', 'aria-label' => '...' }
        .btn-group{ role: 'group' }
          = link_to 'Public', edit_user_registration_path, remote: true, class: 'btn btn-default',
            id: 'btn-to-public-section', role: 'button'
        .btn-group{ role: 'group' }
          %button{ class: 'btn btn-danger active' } Privacy
    .col-lg-10.col-lg-offset-1.col-md-10.col-md-offset-1.col-sm-10.col-sm-offset-1.col-xs-12
      = form_for resource, as: resource_name, url: registration_path(resource_name),
        html: { method: :put, class: 'devise-registrations-edit-privacy-form' } do |f|

        .form-group
          = f.label :email, class: 'text-muted'
          = f.email_field :email, class: 'form-control'

        - if devise_mapping.confirmable? && resource.pending_reconfirmation?
          Currently waiting confirmation for: #{resource.unconfirmed_email}

        .form-group.has-feedback
          = f.label 'New Password', class: 'text-muted'
          = f.password_field :password, autocomplete: 'off', class: 'form-control'
          %span.help-block leave blank if you don't want to change it

        .form-group
          = f.label 'Confirm new password', class: 'text-muted'
          = f.password_field :password_confirmation, autocomplete: 'off', class: 'form-control'

        .form-group.has-warning.has-feedback
          = f.label 'Current password', class: 'control-label'
          = f.password_field :current_password, autocomplete: 'off', class: 'form-control'
          %span.glyphicon.glyphicon-warning-sign.form-control-feedback
          %span.help-block we need your current password to confirm your changes

        = f.submit 'Submit', class: 'btn btn-primary btn-block'
      %h5.text-center.text-muted OR
      = button_to 'Cancel my account', registration_path(resource_name),
        data: { confirm: 'Are you sure?' }, method: :delete,
        class: 'btn btn-danger btn-block', style: 'margin-bottom: 30px;'
