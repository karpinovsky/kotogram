<%- if user_signed_in? && current_user == user_by_profile_username %>
  <%- if current_user.posts.any? %>
    <button class="users-show-header-info-top-new-post-btn">
      New Post
    </button>
  <% end %>
<% end %>
